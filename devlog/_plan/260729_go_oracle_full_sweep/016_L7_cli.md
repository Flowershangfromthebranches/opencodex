# 016 — L7 인벤토리: `src/cli/**`

33파일 / 8,268줄. 대응 go `internal/cli`(77파일) + `go/cmd`.

## 핵심 발견: 최상위 명령은 다 있고, 서브커맨드 층이 비어 있다

`cli/index.ts:718-1099`의 최상위 명령은 `cli.go:48-99`에 대부분 등록돼 있다. 격차는 그 아래
계층이고, 대부분 **백엔드는 이미 있는데 CLI 디스패치만 없는** 모양이다.

### `models` 런타임 서브커맨드 (7건)

| 명령 | 오라클 | go | 등급 |
| --- | --- | --- | --- |
| `models live [--provider]` | `models-runtime.ts:37` | ABSENT (`models.go:36`) | MISSING |
| `models edit <custom-id>` | `:50` | 백엔드 존재, CLI 없음 | UNWIRED |
| `models enable/disable [--native]` | `:86` | model-visibility 백엔드 존재 | UNWIRED |
| `models provider <name> <on\|off>` | `:101` | 같음 | UNWIRED |
| `models selected <provider>` | `:119` | selected-models 백엔드 존재 | UNWIRED |
| `models context <...>` | `:144` | provider-context-caps 백엔드 존재 | UNWIRED |
| `models shadow <...>` | `:176` | shadow-call-settings 백엔드 존재 | UNWIRED |

### `provider` 런타임 서브커맨드 (5건)

`provider edit/update`(MISSING), `provider quota`(UNWIRED, 백엔드
`runtime_management.go:57`), `provider presets`(MISSING), `provider account-mode`(UNWIRED),
`provider selected`(UNWIRED). go `provider.go:93-108`은 list/add/remove/show/default/test만 갖는다.

### 나머지

- `debug`: 오라클 `debug.ts:144`는 실행 중 프록시의 `/api/debug`를 다루며 provider/usage/
  injection/claude 스코프와 로그 tail을 지원한다. go `debug.go:9`는 **로컬 설정 플래그를
  변경**한다 — 다른 기능이다 (DIVERGENT).
- `doctor --fix-codex-runtime`: 오라클 `doctor.ts:637`. go는 `--json`만 받는다 (MISSING).
- help 텍스트가 위 부재를 반영해 오라클과 다르다 (DIVERGENT ×2).

## go 전용 확장 (오라클 역행 아님, 기록만)

`serve` 별칭, 최상위 `diagnostics`/`diagnose`, `completion`, 최상위 `claude-desktop`,
`models efforts`/`effort-ladder`, `account add --json-stdin`, `config path`.

## 스윕 결과

`internal/cli`에는 참조 1개(선언뿐)인 exported 심볼이 없다. `ProviderQuotas`는 참조 12개로
백엔드에서는 살아 있으나 CLI 진입점이 없다 — 위 표의 UNWIRED가 그것이다.

## OK로 확인된 명령군

`combo`/`route combo`, `agent`, `observe`(+ logs/usage/storage/memory 별칭), `access`/`api-key`,
`system`, `config` 파리티 표면, `account` 인증 흐름(login/reauth/code/cancel/reset-credits).

## 검증하지 않은 것

상태 변경 명령(login/install/service)은 실행하지 않았다. doctor 출력의 바이트 단위 일치는
확인하지 않았고 `--fix-codex-runtime` 부재만 확정했다.
